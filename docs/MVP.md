# MVP Matrix

MVP matrix stand for `Model` `View` and `Projection` for translate 3D object to the screen 2D

## Model Matrix(M)

process position, shape, and perspective(corner) for rotate, translate, and scale the object.

## View Matrix(V)

setup camera for view object.

## Projection Matrix(P)

process Z axis in the screen rendering to see multiple object.
